### API creation for Authentication

  # POST /registration => user create / register karna
  # POST /login => user login karna userName aur password ke sath
  # GET /user => user ki details lena [Protected]
  # GET /logout => user logout karna [Protected]